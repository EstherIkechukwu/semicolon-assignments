pseudo code:2.26
stage one:
- prompt the user to enter two integers.
- Determine whether the first number tripled is a multiple of the second number doubled.
- display the result.

Stage Two: 
- Prompt the user to enter first integer
- collect first integer
- store as "firstInteger" 

- Prompt the user to enter second integer.
- collect second integer.
- store as "secondInteger".

- Calculate the first number using "firstIntger" * "firstIntger" * "firstIntger"
- collect the result.
- store as "tripledFirstInteger"

- Calculate the second number using "secondInteger" * "secondInteger"
- collect the number.
- store as "doubledSecondInteger"

- If ("tripledFirstinteger" % "doubledSecondInteger" == 0)
- Collect the result 
- Display the result.

- If ("tripledFirstInteger" % "doubledSecondInteger" != 0)
- collect the result.
- Display the result

